import { createContext } from 'react'

/** Supplemental material may discuss later events; expert training releases it in the debrief. */
export const RestrictedKnowledgeContext = createContext(false)
